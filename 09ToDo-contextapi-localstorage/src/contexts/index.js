// Importing elements (TodoContext, TodoProvider, useTodo) from the "TodoContext" module and re-exporting them

// TodoContext is a context that manages the state related to todos
// It contains the todos array and functions to manipulate them
export { TodoContext, TodoProvider, useTodo } from "./TodoContext";
